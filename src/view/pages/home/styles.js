import { colors } from '@colors';

export const styles = {
    base: {

    },
    
    address: {
        color: colors.midGrey
    },

    cardsWrapper: {
        margin: '32px 0'
    }
}