import { colors } from '@colors';

const base = {
    padding: '12px 16px',
    marginBottom: '16px',
    border: '1px solid ' + colors.softGrey,
    borderRadius: '8px',
    outline: 'none',
    display: 'block'
}

export {
    base
}