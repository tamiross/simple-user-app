import { colors } from '@colors';

export const styles = {
    base: {
        background: colors.primaryBlue,
        paddingTop: 20,
        paddingBottom: 20,
        borderBottom: '1px solid rgba(0,0,0,0.1)'
    }
}